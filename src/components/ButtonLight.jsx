import StyledButtonLight from "../assets/styles/components/StyledButtonLight";

const ButtonLight = ({ children }) => (
  <StyledButtonLight>{children}</StyledButtonLight>
);

export default ButtonLight;
