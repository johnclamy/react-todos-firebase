import styled from "styled-components";
import StyledButton from "./StyledButton";

const StyledButtonInfo = styled(StyledButton)`
  color: var(--darkGray);
  background-color: var(--light);
`;

export default StyledButtonInfo