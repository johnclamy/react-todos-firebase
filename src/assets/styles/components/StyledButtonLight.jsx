import styled from "styled-components";
import StyledButton from "./StyledButton";

const StyledButtonLight = styled(StyledButton)`
  color: var(--darkGray);
  background-color: var(--lighter);
`;

export default StyledButtonLight;
