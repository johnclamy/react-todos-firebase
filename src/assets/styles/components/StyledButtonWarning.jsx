import styled from "styled-components";
import StyledButton from "./StyledButton";

const StyledButtonWarning = styled(StyledButton)`
  color: var(--white);
  background-color: var(--warning);
`;

export default StyledButtonWarning;
