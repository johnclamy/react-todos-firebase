import styled from 'styled-components'

const StyledTodosHeader = styled.header`
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: 1rem;
`

export default StyledTodosHeader